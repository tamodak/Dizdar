<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Dizdar lists every installed app so the user can choose what to block, so a <queries>
        declaration cannot express what it needs. Google Play restricts this permission, but
        Dizdar is provisioned as device owner by adb, Shizuku or QR rather than shipped on Play.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- Optional provisioning path: Shizuku supplies shell privileges without a PC. -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!--
        Companion pairing works by scanning QR codes off another phone's screen. As device owner
        Dizdar grants itself this permission, so the runtime prompt is only a fallback.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- A device without a camera can still be a locked device; it just cannot be a companion. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!--
        allowBackup is false because Dizdar's state is meaningless off this device: the credential
        record and the peer list are verified against a Keystore key that never leaves the hardware,
        so a restored copy is a lock with no key. The two rules files are kept and point at
        all-exclude rules anyway, so the intent survives someone flipping this attribute.

        tools:ignore="DataExtractionRules" silences lint's insistence on per-domain rules, which is
        moot while backup is off entirely.
    -->
    <application
        android:name=".DizdarApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dizdar"
        tools:ignore="DataExtractionRules">
        <!--
            The only user-facing activity; every screen is Compose inside it.

            adjustResize so the password entry stays visible above the soft keyboard — it is the one
            input in the app that opens one.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Dizdar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Required for QR / Android Enterprise provisioning on Android 11+. The system
            asks the DPC which provisioning mode it supports; without this activity the
            QR flow fails outright.

            Theme.NoDisplay because it answers with an intent extra and finishes without drawing.
            Guarded by BIND_DEVICE_ADMIN so only the system can start it.
        -->
        <activity
            android:name=".admin.provisioning.GetProvisioningModeActivity"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.app.action.GET_PROVISIONING_MODE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            Shown once by the system after provisioning completes, so the DPC can confirm the device
            is compliant. Dizdar has nothing to enforce at this point, so it acknowledges and hands
            control back — reaching it is the clearest signal that QR provisioning worked end to end.

            Guarded by BIND_DEVICE_ADMIN so only the system can start it.
        -->
        <activity
            android:name=".admin.provisioning.AdminPolicyComplianceActivity"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:theme="@style/Theme.Dizdar">
            <intent-filter>
                <action android:name="android.app.action.ADMIN_POLICY_COMPLIANCE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            The admin component. Dizdar uses none of the classic device-admin policies, but a device
            owner has to *be* an admin component: this receiver's flattened name is what gets passed
            to `dpm set-device-owner`, embedded in the QR payload, and handed to every
            DevicePolicyManager call as the calling admin.
        -->
        <receiver
            android:name=".admin.DizdarDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>

            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
        </receiver>

        <!--
            Shizuku's own provider, which brokers the connection to its server. Declared verbatim as
            the library requires; multiprocess is false because Dizdar runs in one process — the
            privileged user service lives in Shizuku's process, not Dizdar's.
        -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>
